
// Directive on C preprocessor before the program has compiled
#include <stdio.h>

// Starting a main function for returning that is inside in this function
int main() {

    // "printf" is a command for output a variable, string, or even a function in the same main function
    printf("Hello world!\n");


    return 0; // Indicates that the program completed successfully
}
